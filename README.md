# fit_update

Strength Identity — a plain HTML/CSS/JS fitness web app.

## Run as iPhone App

To build and open the iOS Xcode project, run:

```bash
npm install
npx cap sync ios
npx cap open ios
```

Then build and run from Xcode on a simulator or connected device.
